class recurs
{
    static int Main(string[] args)
    {

    }

    static bool isNumbersEven(int n)
    {
        if(n.Length <= 0)
            return true;



    }

    static int[] SOPEx(int[] arr)
    {
        int[] arr2 = new int[];
        int j = 0;

        for(i = 0 , i <= arr.Length , i++)
        {
            if(arr[i]%2 == 0 || arr[i]%3 == 0)
                arr2[j] = arr[i];
                j++;
        }
        return arr2;
    }

    static sumOfPrimes(int[] arr, int i)
    {
        arr2 = SOPEx(arr);

    }
}